{
  "name": "alert-hub-root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "npm run dev --prefix alerthub",
    "build": "npm run build --prefix alerthub",
    "preview": "npm run preview --prefix alerthub"
  }
}
